#include"AES1.h"
AES::AES()
{
	int i, j;
	for (i = 0; i < 16; i++)
	{
		cipherKey[i] = '0';
	}
	for (i = 0; i < 4; i++) {
		for (j = 0; j < 4; j++) {
			plainText[i].wordKey[j] = '0';
			cipherText[i].wordKey[j] = '0';
			deCipherText[i].wordKey[j] = '0';
		}
	}
}
void AES::setPlainText(byte pla[])//设置明文
{
	int i;
	for (i = 0; i < 16; i++)
	{
		plainText[i % 4].wordKey[i / 4] = pla[i];
		//plain是字，第一个字节存储在[0][0],第二个在[0][1]...一直[3][3];一共16组

	}
}

void AES::setCipherKey(byte key[]) //设置初始密钥并扩展
{
	int i;
	for (i = 0; i < 16; i++)
	{
		cipherKey[i] = key[i];
	}
	keyExpansion(cipherKey, WordKey);//扩展密钥
}

void AES::initRcon()//密钥扩展常量的初始化
{
	int i, j;
	for (i = 0; i < 11; i++)
	{
		for (j = 0; j < 4; j++)
		{
			Rcon[i].wordKey[j] = 0x0;
		}
	}
	Rcon[1].wordKey[0] = 0x1;
	Rcon[2].wordKey[0] = 0x2;
	Rcon[3].wordKey[0] = 0x4;
	Rcon[4].wordKey[0] = 0x8;
	Rcon[5].wordKey[0] = 0x10;
	Rcon[6].wordKey[0] = 0x20;
	Rcon[7].wordKey[0] = 0x40;
	Rcon[8].wordKey[0] = 0x80;
	Rcon[9].wordKey[0] = 0x1b;
	Rcon[10].wordKey[0] = 0x36;

}

const byte AES::mixColumnMatrix[4][4] = 
{
	{ 0x02,0x03,0x01,0x01 },
	{ 0x01,0x02,0x03,0x01 },
	{ 0x01,0x01,0x02,0x03 },
	{ 0x03,0x01,0x01,0x02 },
};//列混淆矩阵

const byte AES::invMixColumnMatrix[4][4] = {
	{ 0x0e,0x0b,0x0d,0x09 },
	{ 0x09,0x0e,0x0b,0x0d },
	{ 0x0d,0x09,0x0e,0x0b },
	{ 0x0b,0x0d,0x09,0x0e },
};//列混淆逆矩阵

const byte AES::SBox[16][16]=                          //s盒矩阵
{ 
 
};
const byte AES::invSBox[16][16] =                  //s盒逆矩阵
{
 
};

void AES::keyExpansion(byte key[], word w[])//轮密钥扩展函数
{
	int i = 0;
	int j, k;
	word temp;
	while (i < Nk)//计算第0论密钥（即输入的密钥赋值给w[i].wordKey[j]
	{
		for (j = 0; j < 4; j++)
		{
			w[i].wordKey[j] = key[j + 4 * i];

		}
		i++;
	}
	i = Nk;
	while (i < Nb*(Nr + 1))//1-10轮密钥
	{
		temp = w[i - 1];
		if((i%Nk)==0)
		{
			temp = rotWord(temp);//字中四个字节循环左移一位
			temp = subWord(temp);//利用S盒将实现字节替换
			temp = wordXOR(temp, Rcon[i / Nk]);//实现字与Rcon的异或
		}
		/*else if(Nk>6 && (i<i%Nk)==4)//处理其他情况
		{
			temp = subWord(temp);
		}*/
		w[i] = wordXOR(w[i - Nk], temp);
		i++;
	}
	word tempState[44];
	for (i = 0; i < 11; i++)//将密钥的处理方式改为AES计算过程中的状态矩阵
	{
		for (j = 0; j < 4; j++)
		{
			for (k = 0; k < 4; k++)
			{
				tempState[j + i * 4].wordKey[k] = w[k + i * 4].wordKey[j];
			}
		}
	}
	for (i = 0; i < 44; i++)
	{
		for (j = 0; j < 4; j++)
		{
			w[i].wordKey[j] = tempState[i].wordKey[j];
		}
	}
}

word AES::rotWord(word w)//字中四个字节循环左移一位
{
	int i;
	word temp;
	for (i = 0; i < 4; i++)
	{
		temp.wordKey[(i + 3) % 4] = w.wordKey[i];//左移一位相当于右移3位
	}
	return temp;
}

word AES::subWord(word w)//s盒代换
{
	int i;
	byte L, R;
	for (i = 0; i < 4; i++)
	{
		L = w.wordKey[i] >> 4;//获取高四位作为行标
		R = w.wordKey[i] && 0xF;//获取低四位作为列标
		w.wordKey[i] = SBox[L][R];//S盒代换
	}
	return w;
}

word AES::wordXOR(word w1, word w2)//两个字的异或运算（因为字定义为数组，所有需要函数单独求
{
	int i;
	word temp;
	for (i = 0; i < 4; i++)
	{
		temp.wordKey[i] = w1.wordKey[i] ^ w2.wordKey[i];//每个字节进行异或运算
	}
	return temp;
}

void AES::encryption(word in[], word out[], word key[])//加密过程函数
{
	int i, j, k;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			out[i].wordKey[j] = in[i].wordKey[j];//输入赋给输出
		}
	}
	addRoundKey(out, 0);//第0轮只有轮密钥加运算
	for (i = 0; i < 10; i++)
	{
		subByte(out);//字节变换
		shiftRows(out);//行移位
		mixColumn(out);//列混淆
		addRoundKey(out, i);//论密钥加
	}
	subByte(out);
	shiftRows(out);
	addRoundKey(out, i);//第10轮没有列混淆
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			cipherText[i].wordKey[j] = out[i].wordKey[j];//输入赋给输出
		}
	}
}

void AES::subByte(word in[])//字节变换函数与字变换相同，只是没有返回值
{
	int i, j;
	byte L, R;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			L = in[i].wordKey[j] >> 4;
			R = in[i].wordKey[j] & 0xF;
			in[i].wordKey[j] = SBox[L][R];
		}
	}
}

void AES::shiftRows(word in[])//行移位
{
	int i, j;
	word temp[4];
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{

			temp[i].wordKey[(j + (4 - i)) % 4] = in[i].wordKey[j];//除第一行不移位，第i行移动i位,左移i位相当于右移4-i位

		}
	}
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			in[i].wordKey[j] = temp[i].wordKey[j];
		}
	}
}

void AES::mixColumn(word in[])      //列混淆
{
	word re[4];
	int i, j, k;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			re[i].wordKey[j] = GFMultiplyByte(mixColumnMatrix[i][0], in[0].wordKey[j]);
			for (k = 1; k < 4; k++)
			{
				re[i].wordKey[j] ^= GFMultiplyByte(mixColumnMatrix[i][k], in[k].wordKey[j]);
			}
		}
	}
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			in[i].wordKey[j] = re[i].wordKey[j];
		}
	}
}

byte AES::GFMultiplyByte(byte L, byte R)//有限域上的乘法运算
{
	byte temp[8];
	byte re = 0x00;
	temp[0] = L;
	int i;
	for (i = 1; i < 8; i++)
	{
		if (temp[i - 1] >= 0x80)
		{
			temp[i] = (temp[i - 1] << 1) ^ 0x1b;//b7=1,该字节每位与00011011异或

		}
		else
		{
			temp[i] = temp[i - 1] << 1;//b7=0;则左移一位
		}
	}
	for (i = 0; i < 8; i++)
	{
		if (int((R >> i) & 0x01) == 1)
		{
			re ^= temp[i];
		}
	}
	return re;
}

void AES::addRoundKey(word in[], int round)//轮密钥加函数
{
	int i, j;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			in[i].wordKey[j] ^= WordKey[i + 4 * round].wordKey[j];//轮密钥加,第几轮round就是几
		}
	}
}

void  AES::decryption(word in[], word out[], word key[])//解密函数
{
	int i, j;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			out[i].wordKey[j] = in[i].wordKey[j];//输入赋给输出
		}
	}
	addRoundKey(out, 0);//第0轮只有轮密钥加运算
	for (i = 9; i > 0; i--)//密钥顺序由9-1；使用1顺序与加密相反
	{
		
		invShiftRows(out);//行移位逆变换
		invSubByte(out);//字节变换逆变换
		addRoundKey(out, i);//轮密钥加
		invMixColumn(out);//列混淆逆变换
		
	}
	invShiftRows(out);//行移位逆变换
	invSubByte(out);
	addRoundKey(out, i);//第0轮
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			deCipherText[i].wordKey[j] = out[i].wordKey[j];//输入赋给输出
		}
	}
}

void AES::invShiftRows(word in[])//行移位求逆
{
	int i, j;
	word temp[4];
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{

			temp[i].wordKey[(j + i)% 4] = in[i].wordKey[j];//与行移位相反，这里是右移，因此第i行直接加i

		}
	}
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			in[i].wordKey[j] = temp[i].wordKey[j];
		}
	}
}
void AES::invSubByte(word in[])//与字节代换方法相似,不过SBox换成了invSBox
{
	int i, j;
	byte L, R;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			L = in[i].wordKey[j] >> 4;
			R = in[i].wordKey[j] & 0xF;
			in[i].wordKey[j] = invSBox[L][R];
		}
	}
}

void AES::invMixColumn(word in[])//列混淆求逆，与列混淆函数相似，只有矩阵变成了invMixColumnMatrix
{
	word re[4];
	int i, j, k;
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			re[i].wordKey[j] = GFMultiplyByte(invMixColumnMatrix[i][0], in[0].wordKey[j]);
			for (k = 1; k < 4; k++)
			{
				re[i].wordKey[j] ^= GFMultiplyByte(invMixColumnMatrix[i][k], in[k].wordKey[j]);
			}
		}
	}
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++)
		{
			in[i].wordKey[j] = re[i].wordKey[j];
		}
	}
}
void AES::showResult() {
	int i, j;
	cout << "key=";
	for (i = 0; i < 16; i++)
	{
		cout << cipherKey[i];
	}
	cout << endl;
	cout << "plainText=";
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++) {
			cout << plainText[i].wordKey[j];
		}
	}
	cout << endl;
	cout << "cipherText=";
	encryption(plainText, cipherText, WordKey);
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++) {
			cout << cipherText[i].wordKey[j];
		}
	}
	cout << endl;
	cout << "decipherText=";
	decryption(cipherText, deCipherText, WordKey);
	for (i = 0; i < 4; i++)
	{
		for (j = 0; j < 4; j++) {
			cout << deCipherText[i].wordKey[j];
		}
	}
	cout << endl;
}
int main1()
{
	AES a;
	byte plaintxt[] = "abquequweqwreqwy";
	byte key[] = "gsgjagwu28whagxb";
	a.setCipherKey(key);
	a.setPlainText(plaintxt);
	a.showResult();
	system("pause");
	return 0;
}
